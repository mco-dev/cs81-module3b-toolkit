function double(n) {}
The function double(n) will take an integer and return the integer multiplied by 2. It does not check for input. If the input is not a number, the results will more than likely not be expected.

function isEven(n) {}
To find out if the input is an even. If the result does not have a remainder, then the input is considered to be even.

function isOdd(n) {}
This function returns the result of having a remainder present. If the input results in anything other than zero, then it is considered to be odd.

function square(n) {}
This takes a single input and returns the input multiplied by itself.

function multiply(a, b) {}
There are two inputs for this function. It takes them both and returns the results of multiplying them together. If the second integer is zero, it just returns 0.